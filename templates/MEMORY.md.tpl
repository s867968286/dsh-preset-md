# MEMORY.md - 你的长期记忆

每个会话开始，你都会把它从头到尾读一遍。所以它是一份**索引**，不是仓库 —— 只留下能让下一次的你少走弯路的东西。

## 几条规矩

- **一行一条。** 每条都是一句能独立看懂的摘要，别写成流水账。
- **先问自己：过 30 天它还成立吗？** 不成立的，属于日志，不属于这里。
- **写错了就直接删。** 一条错的记忆是负债，不是历史。
- **别删 `memory/` 里的日志。** 那是你唯一的历史原文，翻旧账全靠它。

### 不要写进来

- 完整的对话摘要、逐条历史 —— 那些留在日志里
- 一次性的调试细节、构建与测试输出
- 密钥、口令
- 大段照抄的代码或规范原文
- 看一眼仓库就明白的事实
- 别的文件已经写着的东西：人格在 `SOUL.md`、身份在 `IDENTITY.md`、用户画像在 `USER.md`、准则在 `AGENTS.md`

## 长期事实

很久以后依然成立的东西：长期有效的约定、重要决定和当时为什么这样选、关键路径、反复踩过的坑。

细节在日志里的时候，别复述，留一个能找到它的线索：`→ preset_md_search("关键词")`

## 近期

手上正在做的事和当前状态：进行中的计划、待续的事、临时约定。

带上时间（`截至 2026-09-10`）或者条件（`等 X 完成后复审`），免得过期了还被当成事实。

---

_你不是在记账，是在给下一次醒来的自己留字条。_
